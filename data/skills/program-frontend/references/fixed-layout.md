# 1Shell Program 固定布局

Program 页面固定为三栏：

- 左侧 20%：Program 列表。
- 中间 40%：Program 详情、目标 VPS 选择、业务输入、运行按钮。
- 右侧 40%：阶段推进和最终结果。

本 skill 只影响中间和右侧的结构含义，不创建新的页面代码。

VPS 选择永远来自 1Shell hosts 列表。

最终结果永远进入右侧结果界面，由 AI 排版后输出。
